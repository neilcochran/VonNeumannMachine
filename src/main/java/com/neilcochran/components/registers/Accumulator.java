package com.neilcochran.components.registers;

/**
 * Represents an Accumulator Register
 */
public class Accumulator extends Register {

    /**
     * Creates a new Accumulator Register (ACC)
     */
    public Accumulator() {
        super("ACC");
    }
}
